# на основі якого образу будувати ваш образ
FROM node:22-bookworm-slim
 
# у контейнері створити папку server у руті
RUN mkdir /server

# нова робоча директорія у контейнері
WORKDIR /server

# копіює всі файли зі вказаного шляху на хості у вказаний шлях у образі
COPY ./ ./

# додаємо node_modules до контейнеру
RUN npm install

# кажемо що цей порт у контейнері буде щось слухати
EXPOSE 5000

# команда яку запускає контейнер при старті
CMD [ "npm", "start" ]
